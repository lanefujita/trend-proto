import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App/';
import './index.css';

ReactDOM.render(
  //instantiating the app class (index.js) as a component
  <App />,
  document.getElementById('root')
);
